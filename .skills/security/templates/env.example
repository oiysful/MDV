{
  "name": "my-project",
  "version": "1.0.0",
  "description": "Your project description",

  "# SECURITY NOTE": "Never put real values here. Use actual environment variables.",

  "# Required environment variables": {
    "DATABASE_URL": "your_database_connection_string",
    "JWT_SECRET": "generate_with: node -e \"console.log(require('crypto').randomBytes(32).toString('hex'))\"",
    "JWT_REFRESH_SECRET": "different_random_secret_256_bits",
    "NEXTAUTH_SECRET": "generate_with: openssl rand -base64 32",
    "NEXTAUTH_URL": "https://yourdomain.com"
  },

  "# Add below your actual variables": {}
}
