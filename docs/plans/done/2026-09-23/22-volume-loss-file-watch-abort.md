# 22. 외장·네트워크 볼륨 소실 시 파일 워처 abort 크래시 (SIGABRT)

## 상태
**진단·구현 완료 (2026-09-23, PR #18 머지).** 폴링 범위는 경로 한정으로 결정(2026-09-22, Ian).
구현 중 드러난 것은 아래 "착수 결과" 절.

**실기 확인 완료 (2026-09-23, Ian).** SMB 공유의 `.md`를 연 상태에서 인터넷 연결을 끊었고,
앱은 크래시하지 않았다. 원래 크래시가 난 것과 같은 부류의 소실(네트워크 공유의 vnode가
리보크되는 상황)이므로, 이것으로 "abort 경로를 타지 않는다"가 "그 조건에서 실제로 살아남는다"가
됐다. 아직 해보지 않은 변형은 두 가지다 — 외장 디스크 **물리적 꺼내기**와 **슬립-웨이크
사이클 자체**(원래 크래시는 웨이크 +24초였다). 메커니즘은 같으므로 추가 검증은 선택이다.

2026-09-21 19:00:36에 실제로 발생한 크래시 리포트 1건에서 출발한다. 추정이 아니라 심볼
산술과 libuv 원본으로 크래시 지점을 확정했고, 방아쇠는 세션 상태와 볼륨 마운트 상태로
좁혔다. 수정은 아직 한 줄도 하지 않았다.

## 요약

| 질문 | 답 |
|---|---|
| 어디서 죽었나 | libuv `uv__fs_event`의 **마지막 문장 `abort()`** — kqueue one-shot 파일 워처 재무장 실패. **확정** |
| 왜 그 경로인가 | macOS libuv는 **디렉토리만 FSEvents**로 보고 **파일은 kqueue로 폴백**한다. chokidar 5는 파일마다 `fs.watch(파일)`을 건다. **확정** |
| 무엇이 방아쇠인가 | 슬립/웨이크 직후(웨이크 +24초) **`/Volumes/` 아래 볼륨이 사라진 상태**에서 그 볼륨의 파일 워처에 이벤트가 배달됨. **정황상 확정, errno는 미기록** |
| 앱 코드로 막을 수 있나 | 이벤트 핸들러·try/catch로는 **불가능**하다(네이티브 `abort()`). 그 경로를 **애초에 타지 않게** 하는 것이 유일한 수정 |
| 노출 범위 | `watch-file`만이 아니라 **`watch-directory`도 동일**. chokidar가 디렉토리 스캔 중 만난 파일마다 개별 `fs.watch`를 건다 |

## 1. 크래시 지점 — 확정

```
Exception Type:  EXC_CRASH (SIGABRT) / "abort() called"
Thread 0 CrBrowserMain:  abort ← uv__fs_event+276 ← uv__io_poll ← uv_run
Version: 1.3.0 (1.3.0), macOS 26.6.2, Electron 42
```

설치된 프레임워크(`/Applications/MDV.app/.../Electron Framework`)의 심볼 테이블로 교차검증했다.

```
0x285cf8c  _uv__fsevents_init
0x285d670  _uv__fsevents_close
0x285e308  _uv__fs_event        ← 함수 시작
0x285e41c  _uv_fs_event_init    ← 다음 함수 = uv__fs_event의 끝
```

함수 길이가 정확히 `0x114`(276)바이트이고 크래시 프레임의 복귀 주소가 `+276`, 즉 **함수의
끝**이다. 이는 마지막 명령이 noreturn 호출(`abort`)일 때 생기는 모양이다. `atos`도 같은
주소를 `uv_fs_event_init + 0`으로 돌려준다(같은 사실의 다른 표현이다 — 복귀 주소가 다음
심볼의 첫 바이트).

libuv `v1.x`의 `src/unix/kqueue.c`에서 `uv__fs_event`의 마지막 문장:

```c
/* Watcher operates in one-shot mode, re-arm. */
EV_SET(&ev, w->fd, EVFILT_VNODE, EV_ADD | EV_ONESHOT, fflags, 0, 0);
if (kevent(loop->backend_fd, &ev, 1, NULL, 0, NULL))
  abort();
```

**재무장 실패 = 프로세스 즉사**다. JS 레이어에는 아무 기회도 없다.

## 2. 왜 kqueue 경로를 타는가

`uv_fs_event_start`(kqueue.c)의 분기:

```c
if (uv__fstat(fd, &statbuf))
    goto fallback;
/* FSEvents works only with directories */
if (!(statbuf.st_mode & S_IFDIR))
    goto fallback;          /* ← 파일은 여기서 kqueue로 빠진다 */
```

그리고 chokidar 5는 파일을 파일 그대로 감시한다 — `node_modules/chokidar/handler.js`의
`_handleFile`(339행)이 `_watchWithNodeFs(file, listener)`(393행)를 무조건 호출하고, 그것이
`fs.watch(<파일 경로>)`(126행)다. 부모 디렉토리로 우회하지 않는다.

따라서 `src/main.js:868`의 `watch-file`이 만드는 워처는 **전부** 이 one-shot kqueue 워처다.

크래시 리포트의 Thread 37(`uv__fsevents_*` 영역, CFRunLoop)은 **디렉토리 워처**의 FSEvents
스레드이고 멀쩡히 살아 있었다. 죽은 것은 파일 쪽이다. 두 경로를 섞어 읽지 말 것.

## 3. 방아쇠 — 슬립/웨이크와 사라진 볼륨

| 항목 | 값 |
|---|---|
| Launch / Crash | 2026-09-21 13:46:46 / 19:00:36 (약 5시간 14분 뒤) |
| **Time Since Wake** | **24초** |
| Role | Background |

크래시 직전 마지막으로 저장된 세션(`~/Library/Application Support/MDV/session.json`, 17:25)의
활성 탭이 **`/Volumes/` 아래 외장·네트워크 볼륨의 `.md` 파일**이었다. 크래시 시점(19:00)에는
그 볼륨이 `/Volumes`에 없다(현재도 없다 — 붙었다 떨어지는 볼륨이다).

libuv는 vnode 필터 플래그에 `NOTE_REVOKE`를 함께 건다. 그래서 언마운트/리보크는 **침묵이
아니라 이벤트로 배달된다** → `uv__fs_event` 실행 → 이미 죽은 vnode에 one-shot 재무장 →
`kevent()` 실패 → `abort()`. 웨이크 +24초라는 간격은 이벤트 루프 재개나 공유 재연결
타임아웃과 맞는다.

**보정해 둘 것**(리포트가 말해주지 않는 것):

- `kevent()`의 errno는 크래시 리포트에 남지 않는다. 실패 사유 자체(EBADF/ENOENT 계열)는 추정이다.
- `session.json`은 크래시 1시간 35분 **전** 상태다. "크래시 시점에 그 탭이 열려 있었다"는
  마지막 저장 상태에서의 추론이지 스냅샷이 아니다.
- `DiagnosticReports`의 MDV `.ips`는 1건뿐이다. 이는 "반복 중이 아니다"까지만 말해 준다 —
  리포트는 로테이션되므로 "최초 발생"의 증거는 아니다.

## 4. 노출 범위 — `watch-file` 하나가 아니다

`watch-directory`(`src/main.js:832`)도 같은 지뢰를 깐다. chokidar의 디렉토리 처리는

```
_handleDir → _handleRead → (엔트리마다) _addToNodeFs → _handleFile → fs.watch(파일)
```

이라서, **디렉토리 스캔 중 만난 파일마다 개별 kqueue 워처가 생긴다**. `DIR_WATCH_DEPTH = 1`
이므로 루트와 한 단계 아래의 모든 파일이 대상이다. 탐색기 루트를 `/Volumes/...`로 열면 파일
수만큼 지뢰가 깔린다.

즉 수정은 두 핸들러 모두에 적용해야 한다. 한쪽만 고치면 구멍이 그대로 남는다.

## 5. 수정 설계

착수 순서가 중요하다. **수정 1 → 수정 2** 순으로 간다. 수정 3은 보류다.

### 수정 1. 워처 `error` 핸들러 (선행 필수)

현재 `watch-file`·`watch-directory` 어느 쪽에도 `watcher.on('error', ...)`가 없다. 리스너 없는
EventEmitter의 `error`는 **throw**되므로(메인 프로세스 예외) 이번 크래시와는 **별개의 잠재
크래시**다.

다만 chokidar가 **무엇을 올리고 무엇을 삼키는지**는 실측해서 좁혀 두었다 —
`node_modules/chokidar/index.js:552-561`의 `_handleError`는 `ENOENT`와 `ENOTDIR`을 삼키고,
`ignorePermissionErrors` 기본값 때문에 `EPERM`/`EACCES`도 삼킨다. 즉 **경로가 없어서 나는
실패는 애초에 `error`로 올라오지 않는다.** 리스너가 잡게 되는 것은 `EMFILE`/`ENOSPC` 계열이다.

- 두 핸들러에 `error` 리스너 추가. 로그를 남기고, 해당 경로 구독을 정리한 뒤,
  이미 있는 `directory-watch-unavailable` 계열 통지로 렌더러에 알린다(파일 쪽은 대응 채널이
  없으므로 신설 여부를 결정해야 한다).

> 이 계획의 **첫 판(커밋 `ffc9322`)은 수정 3을 "resume 시 ENOENT가 throw된다"는 이유로
> 수정 1 뒤로 미뤘는데, 그 근거는 위 실측으로 틀린 것으로 밝혀졌다.** ENOENT는 조용히
> 삼켜진다. 수정 3의 실제 위험은 예외가 아니라 **조용히 죽은 워처**다(아래 참조).
> 수정 1과 수정 2는 서로 독립이며, 수정 1은 그 자체로 값을 한다.

### 수정 2. 볼륨 경로는 폴링으로

chokidar의 `usePolling: true`는 `setFsWatchFileListener` → `fs.watchFile` → **uv_fs_poll**
(stat 기반)을 쓴다. kqueue EVFILT_VNODE를 아예 타지 않으므로 `abort()` 경로가 사라진다.
네트워크 공유는 어차피 `fs.watch` 이벤트가 신뢰할 수 없으니 **기능적으로도 이득**이다.

- 감시 경로가 부팅 볼륨 밖(`/Volumes/` 아래)이면 `usePolling: true`(+ `interval`)를 준다.
- `watch-file`과 `watch-directory` 양쪽에 적용한다.
- 판정 기준(경로 접두사)은 테스트가 뒤집을 수 있게 환경변수로 덮어쓸 수 있게 한다 —
  `MDV_TEST_DIR_WATCH_MAX_PATHS`(`src/main.js:773`)가 이미 만들어 둔 선례를 따른다.
  그렇지 않으면 테스트가 실제 볼륨을 마운트해야 해서 검증이 불가능해진다.

**폴링 범위 — 경로 한정으로 결정 (2026-09-22, Ian)**

| 안 | 내용 | 대가 | 판정 |
|---|---|---|---|
| **경로 한정** | `/Volumes/` 아래만 폴링 | 로컬은 기존 성능·즉시성 유지. 위험 부류(vnode 리보크)는 사실상 볼륨 단위이고, **로컬 파일은 unlink 후에도 재무장이 성공한다(실측, 7절 프로브 A)** | **채택** |
| 전면 적용 | 모든 파일 워처를 폴링으로 | 이 크래시 부류를 통째로 제거. 대신 감지 지연 ~100ms + 열린 탭 수만큼 주기적 stat | 기각(기록 보존) |

채택 근거가 프로브 A 하나에 얹혀 있다는 점은 분명히 해 둔다 — A는 **로컬 unlink**까지만
증명한다. 로컬에서 같은 부류의 abort가 한 번이라도 관측되면 이 결정은 전면 적용으로
뒤집어야 하며, 그때는 표의 두 번째 행이 이미 준비된 답이다.

### 수정 3. `powerMonitor` suspend/resume (보류)

슬립 전에 워처를 닫고 웨이크 후 되살리는 방식. **이득이 분명하지 않다** — `resume` 시점에
아직 마운트되지 않은 공유에 다시 `watch`를 걸면 ENOENT가 나는데, 위 실측대로 그것은 예외가
아니라 **침묵**이다. 즉 터지지는 않지만 워처가 조용히 죽은 채 남고, 볼륨이 돌아와도 아무도
알려주지 않는 상태를 새로 만든다(수정 2의 폴링은 반대로 스스로 복구된다 — 프로브 B). 게다가
수동 꺼내기·와이파이 끊김처럼 슬립이 아닌 소실은 못 막고, `suspend` 핸들러가 프리즈 전에
끝난다는 보장도 없다.

→ 수정 1·2를 하고도 재현되면 그때 검토한다. 지금은 하지 않는다.

## 6. 부수 효과 — 볼륨이 빠지면 탭이 "삭제됨"으로 보인다

폴링으로 바꾸면 경로가 사라졌을 때 chokidar가 `unlink`를 올린다. 현재 렌더러는 그것을
`mark-deleted`로 처리해(`src/renderer/workspace.js:23`, `:631`) 탭에 삭제 표시를 붙이고
`이 파일이 외부에서 삭제되었습니다. 저장하면 다시 생성됩니다.` 툴팁을 단다.

내용은 메모리에 남으므로 **데이터 손실은 없다**. 다만 "볼륨이 빠진 것"과 "파일이 지워진 것"이
같은 표시로 보이고, 저장하면 마운트 지점에 파일을 새로 만들 수 있다는 안내가 오해를 부른다.
볼륨 소실을 별도 상태로 가를지는 수정 2 착수 시 함께 정한다(범위를 넓히지 않으려면 이번엔
표시만 유지하고 후속으로 미뤄도 된다).

**실기에서 확인됨 (2026-09-23, Ian).** SMB 공유가 사라졌을 때 탭에 실제로 삭제 표시가 붙었다.
이것으로 아래 "미검증"이라고 적어 둔 것 하나가 닫힌다 — **부모 디렉토리째 사라지는 진짜 볼륨
소실에서도 폴러는 살아서 `unlink`를 올린다.**

**그런데 그 상태의 안내 문구는 틀린 조언이다.** `저장하면 다시 생성됩니다`는 로컬 파일이 정말
지워진 경우엔 맞지만, 볼륨이 사라진 경우엔 ⌘S가 둘 중 하나로 간다. 마운트 포인트 디렉토리까지
없어졌으면 `save-file`(`src/main.js:463`)의 `writeFile`이 ENOENT로 실패해 "저장 실패" 경고가
뜬다 — 성가시지만 안전하다. 마운트 포인트 디렉토리가 빈 채로 남아 있으면 **쓰기가 성공해
부팅 볼륨 위에 유령 파일이 생긴다** — 사용자는 저장했다고 믿는데, 공유가 다시 붙으면 그 위로
마운트되어 내용이 보이지 않는다. macOS가 둘 중 어느 쪽으로 가는지는 공유 없이 확인할 수 없어
단정하지 않는다. 어느 쪽이든 **이 상태에서 그 문구는 하지 말아야 할 행동을 권하고 있다.**

**재마운트 복구**는 공짜로 따라올 가능성이 크다. 폴링 워처는 파일이 돌아오면 `add`를 다시
올리고(실측, 7절 프로브 B), `resolveExternalChangeAction`은 `unlink`가 아닌 이벤트를 깨끗한
탭에서 `reload`로 보내며(`src/renderer/workspace.js:22-26`), `applyExternalContent`가
`deletedExternally = false`로 되돌린다(`:550`). **단 프로브는 파일만 지웠다 — 부모 디렉토리째
사라지는 진짜 볼륨 소실에서도 폴러가 살아 있는지는 미검증이다.** 실기 확인 항목이다.

## 7. 검증

### 이미 돌린 프로브 (2026-09-22)

착수 전에 계획이 기대고 있던 가정 두 개를 먼저 반증 시도했다. 둘 다 살아남았다.

| 프로브 | 방법 | 결과 |
|---|---|---|
| **A. 로컬 파일 unlink는 안전한가** | 로컬 임시 파일에 `fs.watch`(= 문제의 kqueue 경로)를 걸고 unlink 후 재생성 | `["change","rename"]` 수신, **프로세스 생존**(exit 0). 폴링을 `/Volumes/`로 한정하는 근거가 선다 |
| **B. 폴링은 파일이 돌아오면 다시 붙는가** | `usePolling: true`로 같은 파일을 감시하며 unlink → 재생성 | `["change","unlink","add","change"]` — **`add`가 다시 올라온다.** 재마운트 시 탭이 스스로 복구될 경로가 있다 |

**프로브가 덮지 못한 것**: 둘 다 파일만 지웠다. 볼륨 언마운트(vnode 리보크, 부모 디렉토리째
소실)는 실기 없이 재현할 수 없다. A는 "로컬 unlink는 안전하다"까지만 말하며, 그것이 곧
"로컬은 전부 안전하다"는 아니다.

### 착수 시 검증

- **단위/컨트롤러**: 경로 → 워처 옵션 판정 함수를 분리해 `/Volumes/...`는 폴링, 로컬은
  기본값이 나오는지 고정한다. 부팅 없이 검증되는 계층이다.
- **Electron**: 환경변수로 볼륨 접두사를 임시 디렉토리로 덮어쓰고, 그 아래 파일을 연 뒤
  변경이 정상 감지되는지 확인한다(폴링 경로가 기능적으로 동작함을 보장). 기존
  `tests/electron/file-watching.test.js`(7건)와 같은 골격을 쓴다.
- **`error` 핸들러**: 워처에 `error`를 직접 emit시켜 프로세스가 살아남고 통지가 나가는지 본다.
- **실기 확인(대체 불가) — 완료 2026-09-23.** SMB 공유의 `.md`를 연 채 인터넷 연결을 끊었고
  앱은 살아남았다(Ian).
  "수정 전 코드가 같은 조건에서 죽는지"는 따로 재현하지 않았다. **그럴 필요가 없다 — 이 계획서
  자체가 그 증거에서 출발했다.** 2026-09-21의 실제 크래시 리포트가 수정 전 코드가 바로 그
  조건에서 죽은 기록이다. before/after가 양쪽 다 실물로 있다.
  남은 변형(물리적 꺼내기, 슬립-웨이크 사이클)은 같은 메커니즘이라 선택 사항이다.

## 착수 결과 (2026-09-23) — PR #18

### 들어간 것

| 파일 | 내용 |
|---|---|
| `src/watch-options.js` (신규) | 순수 경로 로직. `/Volumes/` 아래는 `usePolling`. `update-checker.js`와 같은 모양(Electron·fs 의존 없음) |
| `src/main.js` | 두 `chokidar.watch`에 옵션 적용 + 없던 `error` 리스너 2개. `__mdvFileWatchers` 테스트 훅(기존 dir 훅과 같은 가드) |
| `src/renderer/explorer.js` | `directory-watch-unavailable`에 `reason`이 붙어 문구가 갈린다 |
| 테스트 | 단위 7건(`watch-options`), Electron 3건(폴링 백엔드+실제 배달, 파일 워처 error, 디렉토리 워처 error) |

### 계획서 범위를 넘은 변경 1건 — 토스트 문구 분기

디렉토리 워처의 error 해체는 경로-개수 가드와 같은 통지 채널을 쓴다. 그대로 재사용하면
자원 고갈(EMFILE)이 **"폴더가 너무 커서"**로 보고된다 — 없는 파일 개수를 찾게 만드는 거짓말이다.
그래서 payload에 `reason`을 실어 `explorer.js`가 문구를 고르게 했다. 가드 쪽 문구는 글자 그대로
유지했으므로 그것을 단언하는 기존 테스트는 그대로 통과한다.

### 프로브 — 착수 전에 확인한 것 (계획서의 가정 검증)

`awaitWriteFinish`와 `usePolling`을 **같이** 쓰는 조합은 7절의 프로브 B가 덮지 않았다. 따로 쟀다.

| 확인 | 결과 |
|---|---|
| 폴링 + `awaitWriteFinish`, 2단 append | `change@510ms` 1건으로 합쳐진다 — 동작한다 |
| 같은 옵션에서 unlink | `unlink@203ms` |
| **쓰기 대기 중** unlink | `unlink@204ms` — `awaitWriteFinish`가 삼키지 않는다 |
| 바이너리(`.png`) 경로 | 동작한다. **그래서 `interval`을 지정하지 않는다** — 지정하면 마크다운만 기본 cadence에서 벗어나고 같은 핸들러가 감시하는 임베드 이미지는 `binaryInterval`(300ms)에 남아 둘이 조용히 갈라진다 |

### 검증 설계가 바뀐 지점

7절은 "워처에 `error`를 직접 emit시켜 프로세스가 살아남는지 본다"고 적었다. 그것만으로는
**동어반복**이다 — 리스너가 무엇을 하든 통과한다. 그래서 관측 가능한 결과를 단언하도록 바꿨고
(파일: 맵에서 경로가 떨어진다, 디렉토리: 해체 + 지정된 문구의 토스트), **지키는 것을 지워서
빨개지는지 확인**했다. 리스너를 지우면 emit이 메인 프로세스에서 그대로 throw된다 —
`evaluate: Error: simulated EMFILE` — 정확히 막으려던 크래시다. 폴링 배선을 지우면 백엔드 단언이
깨진다.

### 남은 것

- `powerMonitor` suspend/resume은 보류 그대로. 계획서 5절의 근거대로, 그리고 폴링 경로는 파일이
  돌아오면 스스로 복구되므로(프로브 B) 더 나은 답이 이미 들어갔다.
- **실기 확인은 아직 안 했다.** 외장 디스크나 SMB 공유의 `.md`를 연 채 꺼내기/슬립-웨이크를
  해 보는 것은 Ian만 할 수 있다. 7절의 "실기 확인(대체 불가)" 항목이 유일하게 열려 있다.
- 6절의 재마운트 복구는 여전히 파일 단위 프로브까지만 확인됐다(부모 디렉토리째 사라지는 조건 미검증).

## 참고

- 원본 크래시 리포트(`crash_report.log`)는 `.gitignore`의 `*.log`에 걸려 리포에 들어가지
  않는다. 머신 식별자와 작업 경로가 들어 있으므로 그대로 커밋하지 말 것. 원본은
  `~/Library/Logs/DiagnosticReports/MDV-2026-09-21-190042.ips`에도 있다(로테이션 대상).
- libuv 원본: `libuv/libuv` `v1.x` `src/unix/kqueue.c`의 `uv__fs_event`, `uv_fs_event_start`.
